# Component Adapter POC

## Geting started
- `yarn install`
- `yarn run dev` 

## How to change adapter
- In order to swap between adapters, code must be changed in both `entry.client.tsx` and `entry.server.tsx`
